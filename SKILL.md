---
name: hello-ui
description: 建立并持续维护网页和 App 项目的六份设计说明，覆盖业务、视觉、组件以及桌面与手机端自适应；用于长期迭代项目的初始化、同步、实现和审查。
---

# 长期设计说明书

在项目根目录维护六份当前有效的设计声明。已有项目先读原文，再读 README、路由、组件、样式与 token；只把用户决定和源码能证实的内容写成事实。未知项标 `[待确认]`。参考网站、截图和示例只提供线索。

| 文档 | 职责 |
| --- | --- |
| `spec.md` | 用户、功能、信息架构、状态流转、验收条件及跨端任务链 |
| `domain.md` | 业务对象、术语、风险和责任边界 |
| `craft.md` | 排版、密度、反馈、动效、触控和指针体验 |
| `design.md` | 语义 token、主题、字体、布局尺度及视觉状态 |
| `components.md` | 组件用途、变体、状态、无障碍和跨端转换 |
| `template.md` | 页面类型、导航壳层、内容优先级、区域与栏位转换 |

## 工作流

1. 初始化、用户要求同步或本次工作完成时运行 `python <skill>/scripts/sync_docs.py --project-root <项目> --mode immediate`。进入已有项目而用户未要求立即同步时运行 `--mode passive`；它记录变更，默认静默 30 分钟，并在下一次调用且达到阈值后收敛受影响文档。脚本不在后台驻留。同步只更新受管区块里的源码路径线索；已有同名文档若无完整标记则保留并报告。随后按上述边界填写已确认事实与设计判断。项目很小时可保持每份文档简短，不填无证据的规则。
2. 每次设计变更先找当前规则及依赖它的组件和页面，再更新归属文档；其他文档只链接，不复制规则。改动跨端布局时按 [响应式契约](references/responsive-contract.md) 记录手机、中间宽度和桌面行为。
3. 中间试验不成为正式规则。用户确认或任务实现验证后，把最终结论写入对应文档。会影响未来选择的决定按 [决策记录](references/decision-records.md) 留下原因、影响和替代方案。
4. 用脚本的 `--check` 检查六份文档和受管标记。检查相关页面与组件的正常、空、加载、错误和禁用状态。至少验证一个窄屏手机和一个桌面视口；相关时补测中间宽度、横屏、长文本、200% 缩放及虚拟键盘。检查键盘、触控、焦点、对比度和减少动效。没有渲染能力时如实说明源码检查范围。

## 设计质量

每条规则写明适用范围、触发条件、可观察结果和证据。断点由内容碰撞或任务变化解释，固定数值须来自代码或明确决定。Token 只保留一个事实源；文档引用实际 token，缺失项列为待补，不用孤立色值或像素值遮掩。历史决定不与当前规则并列。

按 [组件契约](references/component-contract.md) 写重要组件。按 [规范入口](references/spec-contract.md) 保持六份文档的字段一致；按项目规模删去不适用项。除非用户要求，不安装钩子、不修改项目配置或启动后台任务。

交付时说明六份文档中改了什么、依据、手机与桌面行为、验证结果和待确认项。生成物不加署名、推广文字或第三方品牌。

