---
name: hello-ui
description: 为长期迭代项目开发或修改多个 HTML 页面、单页多模块；每个页面用同一份实现适配手机与桌面，并保持跨页面视觉一致。也可维护项目设计说明。
---

# helloUI：跨端页面与风格一致性

日常优先处理实际页面。**一个页面对应同一份 HTML/路由实现**，用响应式布局和交互适配手机与桌面；不要为两端各建一套页面。多个 HTML 文件表示首页、列表页、详情页等不同页面，每个文件都要自行适配两端。同一项目的多个页面或一个页面的多个模块应像同一产品。已有项目先读相关页面、共享 CSS/组件和现有设计规范，沿用已确立的字体、颜色、间距、按钮、导航与状态样式。参考图和参考网站只提供线索；不要把它们的风格直接覆盖项目已有规则。

## 日常页面工作流

1. 找到本次页面与相邻页面、同类模块。识别共享样式入口、CSS 变量、组件、页面壳层和可复用模式；没有显式规范时，从重复出现的实现提炼一个小型风格基线，标清观察与提议。
2. 先确定同一页面在手机和桌面宽度下的内容顺序、导航、主操作、网格/容器与溢出行为，再修改同一份 HTML/CSS/组件。优先用流式布局、媒体查询或项目已有的响应式机制处理差异；核心内容和操作在两端都应可达。复用现有项目结构；多页面优先用共享样式或组件承载共同规则，单页多模块优先用统一变量与组件变体承载共同规则。不要为相似模块复制一组略有差异的颜色、圆角或间距。
3. 新页面应继承产品壳层和语义样式；有合理差异时写出页面角色与例外原因。新模块应遵守所在页面的层级和节奏，同时按内容需要调整布局。使用 [一致性契约](references/consistency-contract.md) 判断哪些必须统一、哪些可变化。
4. 在手机窄屏和桌面宽屏实测受影响页面；当中间宽度是布局转换点时补测。检查横向溢出、长内容、菜单、表单、弹层、状态、键盘焦点和触控。多页面至少抽查一个已有页面与一个新增或修改页面；单页多模块检查模块交界处的排版、间距与状态样式。详见 [响应式契约](references/responsive-contract.md)。
5. 设计规则真正改变后，更新归属文档；普通实现调整无需重写六份文档。交付时指出适配范围、共享规则复用情况、验证过的视口与未验证项。

## 持续维护的设计说明

需要初始化或维护项目规范时，在项目根目录使用以下六份当前有效的设计声明。只把用户决定和源码能证实的内容写成事实；未知项标 `[待确认]`。

| 文档 | 职责 |
| --- | --- |
| `spec.md` | 用户、功能、信息架构、状态流转、验收条件及跨端任务链 |
| `domain.md` | 业务对象、术语、风险和责任边界 |
| `craft.md` | 排版、密度、反馈、动效、触控和指针体验 |
| `design.md` | 语义 token、主题、字体、布局尺度及视觉状态 |
| `components.md` | 组件用途、变体、状态、无障碍和跨端转换 |
| `template.md` | 页面类型、导航壳层、内容优先级、区域与栏位转换 |

## 规范同步

初始化或用户要求同步时运行 `python <skill>/scripts/sync_docs.py --project-root <项目> --mode immediate`。希望延后源码线索更新时使用 `--mode passive`；它记录变更，默认静默 30 分钟，并在下次调用且达到阈值后收敛受影响文档。脚本不驻留后台，只更新受管区块里的源码路径线索，不自动产出设计判断。已有同名文档若无完整标记则保留并报告。用 `--check` 检查六份文档和受管标记。

设计判断写在标记外：每条规则写明适用范围、触发条件、可观察结果和证据。一项规则只在归属文档维护，其他位置链接引用。中间试验不写成当前规则；值得长期保留的改变按 [决策记录](references/decision-records.md) 记录原因和迁移影响。文档字段见 [规范入口](references/spec-contract.md)，重要组件见 [组件契约](references/component-contract.md)。

## 设计质量

断点由内容碰撞或任务变化解释，固定数值须来自代码或明确决定。Token 只保留一个事实源；文档引用实际 token，缺失项列为待补，不用孤立色值或像素值遮掩。历史决定不与当前规则并列。

除非用户要求，不安装钩子、不修改项目配置或启动后台任务。

生成物不加署名、推广文字或第三方品牌。

