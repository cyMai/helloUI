---
name: hello-ui
description: 从现有前端模板和 HTML/CSS 提炼可追溯的候选设计规则，维护一份 DESIGN.md，并在开发多页面或单页多模块时保持风格一致、让同一页面适配手机与桌面。
---

# helloUI

面向长期迭代的前端 demo。一个页面使用同一份 HTML/路由实现，通过响应式布局适配手机与桌面；多个 HTML 文件表示不同功能页面，不是设备版本。项目根目录的 `DESIGN.md` 是**已确认规则**的简明入口。源码和实际页面是观察证据，自动生成的候选报告不是设计规范。

## 建立或更新设计规则

1. 先读现有 `DESIGN.md`（如有）、用户要求、模板页面、共享 CSS/变量和组件。已有明确决策优先于出现次数；单页偶然样式不能自动升级为全站规则。
2. 对静态 HTML/CSS 项目运行 `python <skill>/scripts/extract_design.py --project-root <项目>`。脚本生成 `.hello-ui/evidence.json` 与 `.hello-ui/proposals.md`：记录页面、CSS 变量、重复声明、共享类名、媒体查询和可能的漂移。它不创建或覆盖 `DESIGN.md`，也不调用浏览器。脚本看不到的 CSS-in-JS、构建结果或动态状态，由 Agent 另行检查。
3. 阅读候选报告及其文件、行号证据。在手机窄屏、桌面宽屏和实际布局转换附近渲染相关页面，检查计算后的样式、内容顺序、导航、溢出和交互。没有渲染能力时，标明仅凭源码的结论及未验证项；不要声称完成视觉验证。
4. 区分共享规则、页面例外与历史漂移。只把有依据的当前规则写入或更新 `DESIGN.md`；不确定的意图标 `[待确认]`，冲突列出具体证据。按 [设计文档契约](references/design-contract.md) 保持文档短且可执行。不要把自动报告整段复制进正式规范。

## 开发页面与模块

先读 `DESIGN.md` 和受影响的现有页面，再实现新功能。新页面沿用共享导航、排版、语义颜色、间距和组件状态；同页新模块遵守标题层级、内容节奏与基础组件样式。优先复用项目已有共享 CSS、变量或组件，不为纯 HTML demo 引入用户未要求的框架。需要例外时说明适用范围和原因。详见 [多页面一致性](references/consistency-contract.md) 和 [组件契约](references/component-contract.md)。

同一页面在手机和桌面宽度下保留核心内容与操作；用流式布局、媒体查询或项目已有机制调整栏位、顺序、导航呈现。用内容碰撞而非设备名称解释断点。检查至少一个手机和一个桌面宽度；中间宽度、横屏、长文案或虚拟键盘影响任务时补测。详见 [响应式契约](references/responsive-contract.md)。

普通文案和局部样式调整无需重新提炼规则。新增共享组件、改变视觉语言或发现跨页漂移时再提炼并更新 `DESIGN.md`。对未来可能反复争论的重大选择，可按 [决策记录](references/decision-records.md) 留简短历史；这不是日常必需文件。

## 交付边界

说明提炼了哪些候选、接受或拒绝的依据、实际修改的规则、验证的视口与未验证项。既有项目若有旧版六份设计文档，先阅读并保留；只有用户要求迁移时才合并成 `DESIGN.md`，不静默删除历史。不要安装钩子、修改项目配置、提交代码或自动发布，除非当前任务授权。生成物不加署名或推广文字。
